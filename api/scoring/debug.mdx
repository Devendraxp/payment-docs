---
title: "Score and Rank Gateways for a Payment — GET /scoring/debug"
sidebarTitle: "Score Gateways"
description: "Run the scoring pipeline against a synthetic payment context and inspect each gateway's composite score with a factor-by-factor breakdown."
---

The `GET /scoring/debug` endpoint runs the full scoring pipeline against a synthetic payment context you supply via query parameters and returns each eligible gateway along with its composite score and a factor-by-factor breakdown. You can use this endpoint to understand exactly why a specific gateway is ranked first, to verify that an affinity change had the intended effect, or to reproduce a routing decision from your logs. No authentication is required.

<Tip>
  The scoring engine uses a 5-minute rolling window of live transaction metrics when computing `successScore` and `latencyScore`. The rankings you see here reflect the gateway's **current health**, not just its static baseline configuration. Run the endpoint again after a few minutes to see how scores shift as recent traffic data rolls off.
</Tip>

## Endpoint

```
GET http://localhost:3000/scoring/debug
```

## Query Parameters

<ParamField query="method" type="string" required>
  The payment method to score for. Must be one of `UPI`, `CARD`, `NETBANKING`, or `WALLET`. Gateways that do not support the requested method are excluded from the results.
</ParamField>

<ParamField query="amountMinor" type="integer" required>
  The transaction amount in the **smallest currency unit** (e.g., paise for INR). Must be a positive integer. For example, pass `33000` for ₹330.00. This value determines which amount tier multiplier is applied.
</ParamField>

<ParamField query="bank" type="string">
  The customer's bank name for bank-affinity scoring (e.g., `HDFC`, `ICICI`, `SBI`, `AXIS`). The value is trimmed and uppercased automatically. When omitted, the bank-affinity factor defaults to a neutral score of `50`.
</ParamField>

## Example Request

```bash
curl "http://localhost:3000/scoring/debug?method=UPI&bank=HDFC&amountMinor=33000"
```

## Scoring Weights

The composite score is the weighted sum of six individual factors. Each factor is computed on a 0–100 scale, then multiplied by its weight and summed to produce the final score.

| Factor | Field | Weight |
|--------|-------|--------|
| Recent success rate | `successScore` | 35 % |
| Response latency | `latencyScore` | 25 % |
| Method affinity | `methodScore` | 15 % |
| Bank affinity | `bankScore` | 12 % |
| Amount tier fit | `amountScore` | 8 % |
| Time-of-day penalty | `timeScore` | 5 % |

## Response

<ResponseField name="context" type="object" required>
  The payment context used for scoring, echoed back so you can confirm the inputs.

  <Expandable title="context fields">
    <ResponseField name="paymentMethod" type="string">Payment method passed in the query.</ResponseField>
    <ResponseField name="amountMinor" type="integer">Amount passed in the query.</ResponseField>
    <ResponseField name="bank" type="string | null">Uppercased bank name, or `null` if not supplied.</ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="rankedGateways" type="array" required>
  Gateways ranked from highest to lowest score. Only enabled gateways that support the requested method are included.

  <Expandable title="rankedGateways item fields">
    <ResponseField name="gatewayName" type="string">Internal gateway name (e.g., `"razorpay"`).</ResponseField>
    <ResponseField name="displayName" type="string">Human-readable gateway name.</ResponseField>
    <ResponseField name="score" type="number">Composite score — the weighted sum of all six factors. Higher is better.</ResponseField>
    <ResponseField name="factors" type="object">
      Individual factor scores (each on a 0–100 scale) before weighting is applied.

      <Expandable title="factors fields">
        <ResponseField name="successScore" type="number">Gateway's recent success rate mapped to a 0–100 scale (35 % weight).</ResponseField>
        <ResponseField name="latencyScore" type="number">Gateway's recent p95 latency mapped to a 0–100 scale, where lower latency yields a higher score (25 % weight).</ResponseField>
        <ResponseField name="methodScore" type="number">Gateway's method-affinity multiplier for the requested method, scaled to 0–100 (15 % weight).</ResponseField>
        <ResponseField name="bankScore" type="number">Gateway's bank-affinity multiplier for the supplied bank, scaled to 0–100. Defaults to `50` when no bank is supplied or no affinity entry exists (12 % weight).</ResponseField>
        <ResponseField name="amountScore" type="number">Gateway's amount-tier multiplier for the transaction size, scaled to 0–100 (8 % weight).</ResponseField>
        <ResponseField name="timeScore" type="number">Gateway's time-of-day score for the current UTC hour. Defaults to `50` when no penalty entry exists for the current hour (5 % weight).</ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Example Response

```json
{
  "context": {
    "paymentId": "debug",
    "merchantId": "debug",
    "customerId": "debug",
    "paymentMethod": "UPI",
    "amountMinor": 33000,
    "instrument": {},
    "bank": "HDFC",
    "experimentId": null,
    "variant": null,
    "treatmentConfig": null,
    "banditUsed": false
  },
  "rankedGateways": [
    {
      "gatewayName": "razorpay",
      "displayName": "Razorpay",
      "score": 95.88,
      "factors": {
        "successScore": 87.00,
        "latencyScore": 89.33,
        "methodScore": 120.00,
        "bankScore": 115.00,
        "amountScore": 110.00,
        "timeScore": 50.00
      }
    },
    {
      "gatewayName": "cashfree",
      "displayName": "Cashfree",
      "score": 83.63,
      "factors": {
        "successScore": 83.00,
        "latencyScore": 86.33,
        "methodScore": 110.00,
        "bankScore": 50.00,
        "amountScore": 100.00,
        "timeScore": 50.00
      }
    }
  ]
}
```

## Error Responses

| Status | Description |
|--------|-------------|
| `400 Bad Request` | `method` is not one of the accepted values, or `amountMinor` is not a positive integer. |
